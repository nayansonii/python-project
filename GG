# this is a code for google maps integration to find out the distance between two coordinates

import googlemaps

def calculate_road_distance(origin, destination):
    """
    Calculate the road distance between two points using Google Maps Distance Matrix API.
    
    :param api_key: Your Google API key.
    :param origin: Starting point as a tuple (latitude, longitude).
    :param destination: Ending point as a tuple (latitude, longitude).
    :return: Distance in kilometers.
    """
    gmaps = googlemaps.Client(key="AIzaSyCaMB1P70L67nZjeyUDmSVT9wpYOsAIwYw")
    
    # Convert tuples to string format for API
    origin_str = f"{origin[0]},{origin[1]}"
    destination_str = f"{destination[0]},{destination[1]}"
    
    # Call the Distance Matrix API
    result = gmaps.distance_matrix(origins=origin_str, destinations=destination_str, mode="driving")
    
    # Extract distance value from response (in meters)
    distance_meters = result['rows'][0]['elements'][0]['distance']['value']
    distance_km = distance_meters / 1000  # Convert to kilometers
    print(result)
    return distance_km
    

# Example usage

origin_coords = (28.7041, 77.1025)       # Delhi (Checkpoint 1)
destination_coords = (28.4595, 77.0266)  # Gurgaon (Checkpoint 3)

distance = calculate_road_distance(origin_coords, destination_coords)
print(f"Road distance between origin and destination: {distance:.2f} km") 
